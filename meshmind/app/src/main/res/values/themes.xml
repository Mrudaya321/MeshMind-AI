<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Material 3 Theme) -->
    <style name="Theme.MeshMind" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        <item name="colorOnPrimaryContainer">@color/text_primary</item>

        <!-- Background and surface colors -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorSurfaceVariant">@color/surface_variant_slate</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <!-- Status & Error states -->
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/colorOnError</item>

        <!-- Custom element styles -->
        <item name="materialCardViewStyle">@style/Widget.MeshMind.CardView</item>
        <item name="materialButtonStyle">@style/Widget.MeshMind.Button</item>
    </style>

    <!-- Custom Card Style -->
    <style name="Widget.MeshMind.CardView" parent="Widget.Material3.CardView.Filled">
        <item name="cardBackgroundColor">@color/surface_slate</item>
        <item name="cardElevation">@dimen/elevation_low</item>
        <item name="shapeAppearance">@style/ShapeAppearance.MeshMind.Medium</item>
    </style>

    <!-- Custom Button Style -->
    <style name="Widget.MeshMind.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorOnPrimary</item>
        <item name="shapeAppearance">@style/ShapeAppearance.MeshMind.Small</item>
    </style>

    <!-- Shape Styles -->
    <style name="ShapeAppearance.MeshMind.Small" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">@dimen/corner_radius_small</item>
    </style>
    
    <style name="ShapeAppearance.MeshMind.Medium" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">@dimen/corner_radius_medium</item>
    </style>

    <style name="ShapeAppearance.MeshMind.Large" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerSize">@dimen/corner_radius_large</item>
    </style>
</resources>
